// This file is auto-generated by @hey-api/openapi-ts

import { type InfiniteData, infiniteQueryOptions, queryOptions, type UseMutationOptions } from '@tanstack/react-query';

import { client } from '../client.gen';
import { connectionsConnect, connectionsDelete, connectionsGet, connectionsListAccounts, connectionsListByProfile, connectionsSelect, googleCreateAd, googleCreateAdGroup, googleCreateAdGroupCriterion, googleCreateBudget, googleCreateCampaign, googleCreateCampaignCriterion, googleCreateConversionAction, googleCreateDisplayAd, googleCreateKeyword, googleEditAccount, googleEditAdGroup, googleEditBudget, googleEditCampaign, googleEnableAd, googleEnableAdGroup, googleEnableCampaign, googleEnableKeyword, googleGetAccount, googleGetAd, googleGetAdGroup, googleGetAdTree, googleGetCampaign, googleGetConversionAction, googleGetConversionStatus, googleGetInsights, googleGetKeyword, googleListAdGroupCriteria, googleListAdGroups, googleListAds, googleListAudiences, googleListCampaignCriteria, googleListCampaigns, googleListConversionActions, googleListConversionGoals, googleListConversionRequests, googleListKeywords, googlePauseAd, googlePauseAdGroup, googlePauseCampaign, googlePauseKeyword, googleRemoveAd, googleRemoveAdGroup, googleRemoveAdGroupCriterion, googleRemoveBudget, googleRemoveCampaign, googleRemoveCampaignCriterion, googleRemoveConversionAction, googleRemoveKeyword, googleRunGaql, googleSearchGeoTargets, googleSendConversions, googleSetAdGroupBids, googleSetCampaignGeoSettings, googleSetCampaignTargetingSetting, googleSetConversionGoal, googleSetKeywordBid, googleUpdateConversionAction, googleUploadImageAsset, logsGet, logsList, mediaAbort, mediaComplete, mediaCreate, mediaDelete, mediaGet, mediaList, mediaListParts, mediaSignPart, mediaUpdate, metaAccount, metaAd, metaAds, metaAdset, metaAdsets, metaCampaign, metaCampaigns, metaInsights, type Options, postsCreate, postsDelete, postsGet, postsList, postsUpdate, postsValidate, profilesCreate, profilesDelete, profilesGet, profilesList, profilesUpdate, tiktokCreatorInfo, tokensMint, webhooksCreateEndpoint, webhooksCreatePortal, webhooksDeleteEndpoint, webhooksGetEndpoint, webhooksListEndpoints, webhooksListEventTypes, webhooksPing, webhooksUpdateEndpoint } from '../sdk.gen';
import type { ConnectionsConnectData, ConnectionsConnectError, ConnectionsConnectResponse, ConnectionsDeleteData, ConnectionsDeleteError, ConnectionsDeleteResponse, ConnectionsGetData, ConnectionsGetError, ConnectionsGetResponse, ConnectionsListAccountsData, ConnectionsListAccountsError, ConnectionsListAccountsResponse, ConnectionsListByProfileData, ConnectionsListByProfileError, ConnectionsListByProfileResponse, ConnectionsSelectData, ConnectionsSelectError, ConnectionsSelectResponse, GoogleCreateAdData, GoogleCreateAdError, GoogleCreateAdGroupCriterionData, GoogleCreateAdGroupCriterionError, GoogleCreateAdGroupCriterionResponse, GoogleCreateAdGroupData, GoogleCreateAdGroupError, GoogleCreateAdGroupResponse, GoogleCreateAdResponse, GoogleCreateBudgetData, GoogleCreateBudgetError, GoogleCreateBudgetResponse, GoogleCreateCampaignCriterionData, GoogleCreateCampaignCriterionError, GoogleCreateCampaignCriterionResponse, GoogleCreateCampaignData, GoogleCreateCampaignError, GoogleCreateCampaignResponse, GoogleCreateConversionActionData, GoogleCreateConversionActionError, GoogleCreateConversionActionResponse, GoogleCreateDisplayAdData, GoogleCreateDisplayAdError, GoogleCreateDisplayAdResponse, GoogleCreateKeywordData, GoogleCreateKeywordError, GoogleCreateKeywordResponse, GoogleEditAccountData, GoogleEditAccountError, GoogleEditAccountResponse, GoogleEditAdGroupData, GoogleEditAdGroupError, GoogleEditAdGroupResponse, GoogleEditBudgetData, GoogleEditBudgetError, GoogleEditBudgetResponse, GoogleEditCampaignData, GoogleEditCampaignError, GoogleEditCampaignResponse, GoogleEnableAdData, GoogleEnableAdError, GoogleEnableAdGroupData, GoogleEnableAdGroupError, GoogleEnableAdGroupResponse, GoogleEnableAdResponse, GoogleEnableCampaignData, GoogleEnableCampaignError, GoogleEnableCampaignResponse, GoogleEnableKeywordData, GoogleEnableKeywordError, GoogleEnableKeywordResponse, GoogleGetAccountData, GoogleGetAccountError, GoogleGetAccountResponse, GoogleGetAdData, GoogleGetAdError, GoogleGetAdGroupData, GoogleGetAdGroupError, GoogleGetAdGroupResponse, GoogleGetAdResponse, GoogleGetAdTreeData, GoogleGetAdTreeError, GoogleGetAdTreeResponse, GoogleGetCampaignData, GoogleGetCampaignError, GoogleGetCampaignResponse, GoogleGetConversionActionData, GoogleGetConversionActionError, GoogleGetConversionActionResponse, GoogleGetConversionStatusData, GoogleGetConversionStatusError, GoogleGetConversionStatusResponse, GoogleGetInsightsData, GoogleGetInsightsError, GoogleGetInsightsResponse, GoogleGetKeywordData, GoogleGetKeywordError, GoogleGetKeywordResponse, GoogleListAdGroupCriteriaData, GoogleListAdGroupCriteriaError, GoogleListAdGroupCriteriaResponse, GoogleListAdGroupsData, GoogleListAdGroupsError, GoogleListAdGroupsResponse, GoogleListAdsData, GoogleListAdsError, GoogleListAdsResponse, GoogleListAudiencesData, GoogleListAudiencesError, GoogleListAudiencesResponse, GoogleListCampaignCriteriaData, GoogleListCampaignCriteriaError, GoogleListCampaignCriteriaResponse, GoogleListCampaignsData, GoogleListCampaignsError, GoogleListCampaignsResponse, GoogleListConversionActionsData, GoogleListConversionActionsError, GoogleListConversionActionsResponse, GoogleListConversionGoalsData, GoogleListConversionGoalsError, GoogleListConversionGoalsResponse, GoogleListConversionRequestsData, GoogleListConversionRequestsError, GoogleListConversionRequestsResponse, GoogleListKeywordsData, GoogleListKeywordsError, GoogleListKeywordsResponse, GooglePauseAdData, GooglePauseAdError, GooglePauseAdGroupData, GooglePauseAdGroupError, GooglePauseAdGroupResponse, GooglePauseAdResponse, GooglePauseCampaignData, GooglePauseCampaignError, GooglePauseCampaignResponse, GooglePauseKeywordData, GooglePauseKeywordError, GooglePauseKeywordResponse, GoogleRemoveAdData, GoogleRemoveAdError, GoogleRemoveAdGroupCriterionData, GoogleRemoveAdGroupCriterionError, GoogleRemoveAdGroupCriterionResponse, GoogleRemoveAdGroupData, GoogleRemoveAdGroupError, GoogleRemoveAdGroupResponse, GoogleRemoveAdResponse, GoogleRemoveBudgetData, GoogleRemoveBudgetError, GoogleRemoveBudgetResponse, GoogleRemoveCampaignCriterionData, GoogleRemoveCampaignCriterionError, GoogleRemoveCampaignCriterionResponse, GoogleRemoveCampaignData, GoogleRemoveCampaignError, GoogleRemoveCampaignResponse, GoogleRemoveConversionActionData, GoogleRemoveConversionActionError, GoogleRemoveConversionActionResponse, GoogleRemoveKeywordData, GoogleRemoveKeywordError, GoogleRemoveKeywordResponse, GoogleRunGaqlData, GoogleRunGaqlError, GoogleRunGaqlResponse, GoogleSearchGeoTargetsData, GoogleSearchGeoTargetsError, GoogleSearchGeoTargetsResponse, GoogleSendConversionsData, GoogleSendConversionsError, GoogleSendConversionsResponse, GoogleSetAdGroupBidsData, GoogleSetAdGroupBidsError, GoogleSetAdGroupBidsResponse, GoogleSetCampaignGeoSettingsData, GoogleSetCampaignGeoSettingsError, GoogleSetCampaignGeoSettingsResponse, GoogleSetCampaignTargetingSettingData, GoogleSetCampaignTargetingSettingError, GoogleSetCampaignTargetingSettingResponse, GoogleSetConversionGoalData, GoogleSetConversionGoalError, GoogleSetConversionGoalResponse, GoogleSetKeywordBidData, GoogleSetKeywordBidError, GoogleSetKeywordBidResponse, GoogleUpdateConversionActionData, GoogleUpdateConversionActionError, GoogleUpdateConversionActionResponse, GoogleUploadImageAssetData, GoogleUploadImageAssetError, GoogleUploadImageAssetResponse, LogsGetData, LogsGetError, LogsGetResponse, LogsListData, LogsListError, LogsListResponse, MediaAbortData, MediaAbortError, MediaAbortResponse, MediaCompleteData, MediaCompleteError, MediaCompleteResponse, MediaCreateData, MediaCreateError, MediaCreateResponse, MediaDeleteData, MediaDeleteError, MediaDeleteResponse, MediaGetData, MediaGetError, MediaGetResponse, MediaListData, MediaListError, MediaListPartsData, MediaListPartsError, MediaListPartsResponse, MediaListResponse, MediaSignPartData, MediaSignPartError, MediaSignPartResponse, MediaUpdateData, MediaUpdateError, MediaUpdateResponse, MetaAccountData, MetaAccountError, MetaAccountResponse, MetaAdData, MetaAdError, MetaAdResponse, MetaAdsData, MetaAdsError, MetaAdsetData, MetaAdsetError, MetaAdsetResponse, MetaAdsetsData, MetaAdsetsError, MetaAdsetsResponse, MetaAdsResponse, MetaCampaignData, MetaCampaignError, MetaCampaignResponse, MetaCampaignsData, MetaCampaignsError, MetaCampaignsResponse, MetaInsightsData, MetaInsightsError, MetaInsightsResponse, PostsCreateData, PostsCreateError, PostsCreateResponse, PostsDeleteData, PostsDeleteError, PostsDeleteResponse, PostsGetData, PostsGetError, PostsGetResponse, PostsListData, PostsListError, PostsListResponse, PostsUpdateData, PostsUpdateError, PostsUpdateResponse, PostsValidateData, PostsValidateError, PostsValidateResponse, ProfilesCreateData, ProfilesCreateError, ProfilesCreateResponse, ProfilesDeleteData, ProfilesDeleteError, ProfilesDeleteResponse, ProfilesGetData, ProfilesGetError, ProfilesGetResponse, ProfilesListData, ProfilesListError, ProfilesListResponse, ProfilesUpdateData, ProfilesUpdateError, ProfilesUpdateResponse, TiktokCreatorInfoData, TiktokCreatorInfoError, TiktokCreatorInfoResponse, TokensMintData, TokensMintError, TokensMintResponse, WebhooksCreateEndpointData, WebhooksCreateEndpointError, WebhooksCreateEndpointResponse, WebhooksCreatePortalData, WebhooksCreatePortalError, WebhooksCreatePortalResponse, WebhooksDeleteEndpointData, WebhooksDeleteEndpointError, WebhooksDeleteEndpointResponse, WebhooksGetEndpointData, WebhooksGetEndpointError, WebhooksGetEndpointResponse, WebhooksListEndpointsData, WebhooksListEndpointsError, WebhooksListEndpointsResponse, WebhooksListEventTypesData, WebhooksListEventTypesError, WebhooksListEventTypesResponse, WebhooksPingData, WebhooksPingError, WebhooksPingResponse, WebhooksUpdateEndpointData, WebhooksUpdateEndpointError, WebhooksUpdateEndpointResponse } from '../types.gen';

export type QueryKey<TOptions extends Options> = [
    Pick<TOptions, 'baseUrl' | 'body' | 'headers' | 'path' | 'query'> & {
        _id: string;
        _infinite?: boolean;
        tags?: ReadonlyArray<string>;
    }
];

const createQueryKey = <TOptions extends Options>(id: string, options?: TOptions, infinite?: boolean, tags?: ReadonlyArray<string>): [
    QueryKey<TOptions>[0]
] => {
    const params: QueryKey<TOptions>[0] = { _id: id, baseUrl: options?.baseUrl || (options?.client ?? client).getConfig().baseUrl } as QueryKey<TOptions>[0];
    if (infinite) {
        params._infinite = infinite;
    }
    if (tags) {
        params.tags = tags;
    }
    if (options?.body) {
        params.body = options.body;
    }
    if (options?.headers) {
        params.headers = options.headers;
    }
    if (options?.path) {
        params.path = options.path;
    }
    if (options?.query) {
        params.query = options.query;
    }
    return [params];
};

export const profilesListQueryKey = (options?: Options<ProfilesListData>) => createQueryKey('profilesList', options);

/**
 * List profiles
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `profiles:read` (the response is limited to the profiles the token is scoped to).
 *
 * Lists the profiles under your account, newest first.
 */
export const profilesListOptions = (options?: Options<ProfilesListData>) => queryOptions<ProfilesListResponse, ProfilesListError, ProfilesListResponse, ReturnType<typeof profilesListQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await profilesList({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: profilesListQueryKey(options)
});

const createInfiniteParams = <K extends Pick<QueryKey<Options>[0], 'body' | 'headers' | 'path' | 'query'>>(queryKey: QueryKey<Options>, page: K) => {
    const params = { ...queryKey[0] };
    if (page.body) {
        params.body = {
            ...queryKey[0].body as any,
            ...page.body as any
        };
    }
    if (page.headers) {
        params.headers = {
            ...queryKey[0].headers,
            ...page.headers
        };
    }
    if (page.path) {
        params.path = {
            ...queryKey[0].path as any,
            ...page.path as any
        };
    }
    if (page.query) {
        params.query = {
            ...queryKey[0].query as any,
            ...page.query as any
        };
    }
    return params as unknown as typeof page;
};

export const profilesListInfiniteQueryKey = (options?: Options<ProfilesListData>): QueryKey<Options<ProfilesListData>> => createQueryKey('profilesList', options, true);

/**
 * List profiles
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `profiles:read` (the response is limited to the profiles the token is scoped to).
 *
 * Lists the profiles under your account, newest first.
 */
export const profilesListInfiniteOptions = (options?: Options<ProfilesListData>) => infiniteQueryOptions<ProfilesListResponse, ProfilesListError, InfiniteData<ProfilesListResponse>, QueryKey<Options<ProfilesListData>>, number | Pick<QueryKey<Options<ProfilesListData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<ProfilesListData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                offset: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await profilesList({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: profilesListInfiniteQueryKey(options)
});

/**
 * Create a profile
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `profiles:write` with account-wide profile scope (`all`); a profile-scoped token cannot create.
 *
 * Creates a profile — a client/brand workspace under your account, and the unit connections attach to.
 */
export const profilesCreateMutation = (options?: Partial<Options<ProfilesCreateData>>): UseMutationOptions<ProfilesCreateResponse, ProfilesCreateError, Options<ProfilesCreateData>> => {
    const mutationOptions: UseMutationOptions<ProfilesCreateResponse, ProfilesCreateError, Options<ProfilesCreateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await profilesCreate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Delete a profile
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `profiles:write` scoped to the target profile.
 *
 * Deletes a profile. The profile must have no connections — delete its connections first (and a connection in turn cannot be deleted while a post still publishes through it).
 */
export const profilesDeleteMutation = (options?: Partial<Options<ProfilesDeleteData>>): UseMutationOptions<ProfilesDeleteResponse, ProfilesDeleteError, Options<ProfilesDeleteData>> => {
    const mutationOptions: UseMutationOptions<ProfilesDeleteResponse, ProfilesDeleteError, Options<ProfilesDeleteData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await profilesDelete({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const profilesGetQueryKey = (options: Options<ProfilesGetData>) => createQueryKey('profilesGet', options);

/**
 * Retrieve a profile
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `profiles:read` scoped to the target profile.
 *
 * Retrieves a single profile by id.
 */
export const profilesGetOptions = (options: Options<ProfilesGetData>) => queryOptions<ProfilesGetResponse, ProfilesGetError, ProfilesGetResponse, ReturnType<typeof profilesGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await profilesGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: profilesGetQueryKey(options)
});

/**
 * Update a profile
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `profiles:write` scoped to the target profile.
 *
 * Updates a profile. Send only the fields you want to change; provide at least one.
 */
export const profilesUpdateMutation = (options?: Partial<Options<ProfilesUpdateData>>): UseMutationOptions<ProfilesUpdateResponse, ProfilesUpdateError, Options<ProfilesUpdateData>> => {
    const mutationOptions: UseMutationOptions<ProfilesUpdateResponse, ProfilesUpdateError, Options<ProfilesUpdateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await profilesUpdate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const connectionsListByProfileQueryKey = (options: Options<ConnectionsListByProfileData>) => createQueryKey('connectionsListByProfile', options);

/**
 * List a profile's connections
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `connections:read` scoped to the target profile.
 *
 * Lists the external accounts connected to a profile, most recent first. Returns 404 if the profile doesn't exist in your account.
 */
export const connectionsListByProfileOptions = (options: Options<ConnectionsListByProfileData>) => queryOptions<ConnectionsListByProfileResponse, ConnectionsListByProfileError, ConnectionsListByProfileResponse, ReturnType<typeof connectionsListByProfileQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await connectionsListByProfile({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: connectionsListByProfileQueryKey(options)
});

export const connectionsListByProfileInfiniteQueryKey = (options: Options<ConnectionsListByProfileData>): QueryKey<Options<ConnectionsListByProfileData>> => createQueryKey('connectionsListByProfile', options, true);

/**
 * List a profile's connections
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `connections:read` scoped to the target profile.
 *
 * Lists the external accounts connected to a profile, most recent first. Returns 404 if the profile doesn't exist in your account.
 */
export const connectionsListByProfileInfiniteOptions = (options: Options<ConnectionsListByProfileData>) => infiniteQueryOptions<ConnectionsListByProfileResponse, ConnectionsListByProfileError, InfiniteData<ConnectionsListByProfileResponse>, QueryKey<Options<ConnectionsListByProfileData>>, number | Pick<QueryKey<Options<ConnectionsListByProfileData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<ConnectionsListByProfileData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                offset: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await connectionsListByProfile({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: connectionsListByProfileInfiniteQueryKey(options)
});

/**
 * Delete a connection
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `connections:write` scoped to the profile that owns the connection.
 *
 * Disconnects a connection. Returns a deletion marker; returns 404 if it does not exist in your account. Disconnect is unconditional — any scheduled posts that target this connection are not blocked; they fail gracefully at publish time with a `connection_removed` error until you reconnect and reschedule.
 */
export const connectionsDeleteMutation = (options?: Partial<Options<ConnectionsDeleteData>>): UseMutationOptions<ConnectionsDeleteResponse, ConnectionsDeleteError, Options<ConnectionsDeleteData>> => {
    const mutationOptions: UseMutationOptions<ConnectionsDeleteResponse, ConnectionsDeleteError, Options<ConnectionsDeleteData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await connectionsDelete({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const connectionsGetQueryKey = (options: Options<ConnectionsGetData>) => createQueryKey('connectionsGet', options);

/**
 * Retrieve a connection
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `connections:read` scoped to the profile that owns the connection.
 *
 * Retrieves a single connection by id.
 */
export const connectionsGetOptions = (options: Options<ConnectionsGetData>) => queryOptions<ConnectionsGetResponse, ConnectionsGetError, ConnectionsGetResponse, ReturnType<typeof connectionsGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await connectionsGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: connectionsGetQueryKey(options)
});

/**
 * Select a connection account
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `connections:write` scoped to the profile that owns the connection.
 *
 * Activates a pending connection by choosing which external account (e.g. which Meta ad account) it uses. The account must be one reachable through the connection’s OAuth grant; its canonical name + currency are resolved server-side from discovery, so the stored display values can’t be spoofed by the caller.
 */
export const connectionsSelectMutation = (options?: Partial<Options<ConnectionsSelectData>>): UseMutationOptions<ConnectionsSelectResponse, ConnectionsSelectError, Options<ConnectionsSelectData>> => {
    const mutationOptions: UseMutationOptions<ConnectionsSelectResponse, ConnectionsSelectError, Options<ConnectionsSelectData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await connectionsSelect({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const connectionsListAccountsQueryKey = (options: Options<ConnectionsListAccountsData>) => createQueryKey('connectionsListAccounts', options);

/**
 * List a connection’s discoverable accounts
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `connections:read` scoped to the profile that owns the connection.
 *
 * Lists the external ad accounts reachable through this connection’s OAuth grant — the picker shown after OAuth, while the connection is still pending account selection. Returns 404 if the connection does not exist in your account, and 501 if account discovery is not yet supported for the connection’s platform.
 */
export const connectionsListAccountsOptions = (options: Options<ConnectionsListAccountsData>) => queryOptions<ConnectionsListAccountsResponse, ConnectionsListAccountsError, ConnectionsListAccountsResponse, ReturnType<typeof connectionsListAccountsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await connectionsListAccounts({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: connectionsListAccountsQueryKey(options)
});

/**
 * Connect a platform account
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `connections:write` scoped to the target profile.
 *
 * Starts an OAuth connect flow for a platform on this profile. Returns a short-lived session the client uses to authorize; the connection is created once authorization succeeds.
 */
export const connectionsConnectMutation = (options?: Partial<Options<ConnectionsConnectData>>): UseMutationOptions<ConnectionsConnectResponse, ConnectionsConnectError, Options<ConnectionsConnectData>> => {
    const mutationOptions: UseMutationOptions<ConnectionsConnectResponse, ConnectionsConnectError, Options<ConnectionsConnectData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await connectionsConnect({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const mediaListQueryKey = (options?: Options<MediaListData>) => createQueryKey('mediaList', options);

/**
 * List media
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `media:read` (the response is limited to the profiles the token is scoped to).
 *
 * Lists the media assets under your account, newest first. Filter by profile, status, kind, your own external_id, or metadata.
 */
export const mediaListOptions = (options?: Options<MediaListData>) => queryOptions<MediaListResponse, MediaListError, MediaListResponse, ReturnType<typeof mediaListQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await mediaList({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: mediaListQueryKey(options)
});

export const mediaListInfiniteQueryKey = (options?: Options<MediaListData>): QueryKey<Options<MediaListData>> => createQueryKey('mediaList', options, true);

/**
 * List media
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `media:read` (the response is limited to the profiles the token is scoped to).
 *
 * Lists the media assets under your account, newest first. Filter by profile, status, kind, your own external_id, or metadata.
 */
export const mediaListInfiniteOptions = (options?: Options<MediaListData>) => infiniteQueryOptions<MediaListResponse, MediaListError, InfiniteData<MediaListResponse>, QueryKey<Options<MediaListData>>, number | Pick<QueryKey<Options<MediaListData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<MediaListData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                offset: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await mediaList({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: mediaListInfiniteQueryKey(options)
});

/**
 * Upload media
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `media:write` scoped to the target profile.
 *
 * Creates a media asset. Two ways to supply the bytes: (1) omit `source_url` and upload the bytes with the returned multipart `upload` session — chunk the file at `part_size`, sign each part via `POST /media/{id}/multipart/sign`, PUT the bytes, then `POST /media/{id}/multipart/complete` (the direct-upload path; `upload` is non-null and resumable), or (2) supply a public `https` `source_url` and we fetch the bytes server-side, validate, transform, and store them (the import path; `upload` is null). Either way the asset settles through the same probe/transform flow.
 */
export const mediaCreateMutation = (options?: Partial<Options<MediaCreateData>>): UseMutationOptions<MediaCreateResponse, MediaCreateError, Options<MediaCreateData>> => {
    const mutationOptions: UseMutationOptions<MediaCreateResponse, MediaCreateError, Options<MediaCreateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await mediaCreate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Delete media
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `media:write` scoped to the profile that owns the media asset.
 *
 * Deletes a media asset and its stored renditions.
 */
export const mediaDeleteMutation = (options?: Partial<Options<MediaDeleteData>>): UseMutationOptions<MediaDeleteResponse, MediaDeleteError, Options<MediaDeleteData>> => {
    const mutationOptions: UseMutationOptions<MediaDeleteResponse, MediaDeleteError, Options<MediaDeleteData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await mediaDelete({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const mediaGetQueryKey = (options: Options<MediaGetData>) => createQueryKey('mediaGet', options);

/**
 * Retrieve media
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `media:read` scoped to the profile that owns the media asset.
 *
 * Retrieves a media asset, including its per-platform processing status and renditions.
 */
export const mediaGetOptions = (options: Options<MediaGetData>) => queryOptions<MediaGetResponse, MediaGetError, MediaGetResponse, ReturnType<typeof mediaGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await mediaGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: mediaGetQueryKey(options)
});

/**
 * Update media
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `media:write` scoped to the profile that owns the media asset.
 *
 * Updates a media asset (alt text, metadata, external_id) or adds `targets` to pre-warm more platforms. Send only the fields to change.
 */
export const mediaUpdateMutation = (options?: Partial<Options<MediaUpdateData>>): UseMutationOptions<MediaUpdateResponse, MediaUpdateError, Options<MediaUpdateData>> => {
    const mutationOptions: UseMutationOptions<MediaUpdateResponse, MediaUpdateError, Options<MediaUpdateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await mediaUpdate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Sign an upload part
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `media:write` scoped to the profile that owns the media asset.
 *
 * Returns a presigned URL to PUT one part of a multipart upload. Chunk the file at the session’s `part_size` and sign each 1-indexed part; read each part PUT’s `ETag` for `complete`.
 */
export const mediaSignPartMutation = (options?: Partial<Options<MediaSignPartData>>): UseMutationOptions<MediaSignPartResponse, MediaSignPartError, Options<MediaSignPartData>> => {
    const mutationOptions: UseMutationOptions<MediaSignPartResponse, MediaSignPartError, Options<MediaSignPartData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await mediaSignPart({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const mediaListPartsQueryKey = (options: Options<MediaListPartsData>) => createQueryKey('mediaListParts', options);

/**
 * List uploaded parts
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `media:write` scoped to the profile that owns the media asset.
 *
 * Lists the parts already uploaded for this asset’s multipart upload (ascending). Skip re-uploading these to resume after a dropped connection.
 */
export const mediaListPartsOptions = (options: Options<MediaListPartsData>) => queryOptions<MediaListPartsResponse, MediaListPartsError, MediaListPartsResponse, ReturnType<typeof mediaListPartsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await mediaListParts({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: mediaListPartsQueryKey(options)
});

/**
 * Complete an upload
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `media:write` scoped to the profile that owns the media asset.
 *
 * Finishes a multipart upload by assembling the uploaded parts. Send every part as `{ part_number, etag }`. Idempotent: completing an already-finished upload returns the asset.
 */
export const mediaCompleteMutation = (options?: Partial<Options<MediaCompleteData>>): UseMutationOptions<MediaCompleteResponse, MediaCompleteError, Options<MediaCompleteData>> => {
    const mutationOptions: UseMutationOptions<MediaCompleteResponse, MediaCompleteError, Options<MediaCompleteData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await mediaComplete({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Abort an upload
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `media:write` scoped to the profile that owns the media asset.
 *
 * Aborts an in-flight multipart upload and discards its parts. Idempotent: aborting an already-finished or unknown upload still returns the asset.
 */
export const mediaAbortMutation = (options?: Partial<Options<MediaAbortData>>): UseMutationOptions<MediaAbortResponse, MediaAbortError, Options<MediaAbortData>> => {
    const mutationOptions: UseMutationOptions<MediaAbortResponse, MediaAbortError, Options<MediaAbortData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await mediaAbort({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const postsListQueryKey = (options?: Options<PostsListData>) => createQueryKey('postsList', options);

/**
 * List posts
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `posts:read` (the response is limited to the profiles the token is scoped to).
 *
 * Lists the posts under your account, newest first. Filter by profile or your own external_id.
 */
export const postsListOptions = (options?: Options<PostsListData>) => queryOptions<PostsListResponse, PostsListError, PostsListResponse, ReturnType<typeof postsListQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await postsList({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: postsListQueryKey(options)
});

export const postsListInfiniteQueryKey = (options?: Options<PostsListData>): QueryKey<Options<PostsListData>> => createQueryKey('postsList', options, true);

/**
 * List posts
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `posts:read` (the response is limited to the profiles the token is scoped to).
 *
 * Lists the posts under your account, newest first. Filter by profile or your own external_id.
 */
export const postsListInfiniteOptions = (options?: Options<PostsListData>) => infiniteQueryOptions<PostsListResponse, PostsListError, InfiniteData<PostsListResponse>, QueryKey<Options<PostsListData>>, number | Pick<QueryKey<Options<PostsListData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<PostsListData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                offset: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await postsList({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: postsListInfiniteQueryKey(options)
});

/**
 * Create a post
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `posts:write` scoped to the target profile.
 *
 * Composes a post + its per-channel variants. `publish` decides the outcome: `now` publishes immediately, `schedule` publishes at `schedule_at`, `draft` just stores it.
 */
export const postsCreateMutation = (options?: Partial<Options<PostsCreateData>>): UseMutationOptions<PostsCreateResponse, PostsCreateError, Options<PostsCreateData>> => {
    const mutationOptions: UseMutationOptions<PostsCreateResponse, PostsCreateError, Options<PostsCreateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await postsCreate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Delete a post
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `posts:write` scoped to the profile that owns the post.
 *
 * Deletes a post and its variants. Returns a deletion marker; 404 if it does not exist in your account. Already-published posts are not retracted from the platform.
 */
export const postsDeleteMutation = (options?: Partial<Options<PostsDeleteData>>): UseMutationOptions<PostsDeleteResponse, PostsDeleteError, Options<PostsDeleteData>> => {
    const mutationOptions: UseMutationOptions<PostsDeleteResponse, PostsDeleteError, Options<PostsDeleteData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await postsDelete({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const postsGetQueryKey = (options: Options<PostsGetData>) => createQueryKey('postsGet', options);

/**
 * Retrieve a post
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `posts:read` scoped to the profile that owns the post.
 *
 * Retrieves a single post by id, including its per-channel variants and their derived status.
 */
export const postsGetOptions = (options: Options<PostsGetData>) => queryOptions<PostsGetResponse, PostsGetError, PostsGetResponse, ReturnType<typeof postsGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await postsGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: postsGetQueryKey(options)
});

/**
 * Update a post
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `posts:write` scoped to the profile that owns the post.
 *
 * Edits a post and/or transitions its publish state (`draft` / `schedule` / `now`). Valid only while the post is draft or scheduled. Send only the fields to change; providing `variants` replaces the whole set.
 */
export const postsUpdateMutation = (options?: Partial<Options<PostsUpdateData>>): UseMutationOptions<PostsUpdateResponse, PostsUpdateError, Options<PostsUpdateData>> => {
    const mutationOptions: UseMutationOptions<PostsUpdateResponse, PostsUpdateError, Options<PostsUpdateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await postsUpdate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Validate a post
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `posts:read` scoped to the target profile.
 *
 * Runs the exact ownership + publish-readiness checks the create path runs under dry_run, with no side effects. Returns a per-issue verdict (`publishable` + `issues[]`) on 200; a reference to a connection/media outside the profile is a typed 422.
 */
export const postsValidateMutation = (options?: Partial<Options<PostsValidateData>>): UseMutationOptions<PostsValidateResponse, PostsValidateError, Options<PostsValidateData>> => {
    const mutationOptions: UseMutationOptions<PostsValidateResponse, PostsValidateError, Options<PostsValidateData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await postsValidate({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const metaAccountQueryKey = (options: Options<MetaAccountData>) => createQueryKey('metaAccount', options);

/**
 * Retrieve the Meta ad account
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Retrieves the Meta ad account this connection is linked to — name, currency, status, and spend. Monetary fields are in the account currency’s minor units.
 */
export const metaAccountOptions = (options: Options<MetaAccountData>) => queryOptions<MetaAccountResponse, MetaAccountError, MetaAccountResponse, ReturnType<typeof metaAccountQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await metaAccount({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: metaAccountQueryKey(options)
});

export const metaInsightsQueryKey = (options: Options<MetaInsightsData>) => createQueryKey('metaInsights', options);

/**
 * Retrieve Meta ad insights
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Retrieves performance insights for the connection’s Meta ad account, aggregated at the chosen `level` over a date window. Metric values are strings, in the account currency’s units.
 */
export const metaInsightsOptions = (options: Options<MetaInsightsData>) => queryOptions<MetaInsightsResponse, MetaInsightsError, MetaInsightsResponse, ReturnType<typeof metaInsightsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await metaInsights({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: metaInsightsQueryKey(options)
});

export const metaCampaignsQueryKey = (options: Options<MetaCampaignsData>) => createQueryKey('metaCampaigns', options);

/**
 * List Meta campaigns
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Lists the campaigns under the connection’s Meta ad account, with each campaign’s status, objective, and budgets (budgets in the account currency’s minor units).
 */
export const metaCampaignsOptions = (options: Options<MetaCampaignsData>) => queryOptions<MetaCampaignsResponse, MetaCampaignsError, MetaCampaignsResponse, ReturnType<typeof metaCampaignsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await metaCampaigns({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: metaCampaignsQueryKey(options)
});

export const metaCampaignQueryKey = (options: Options<MetaCampaignData>) => createQueryKey('metaCampaign', options);

/**
 * Retrieve a Meta campaign
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Retrieves a single campaign under the connection’s Meta ad account by its campaign id — status, objective, and budgets.
 */
export const metaCampaignOptions = (options: Options<MetaCampaignData>) => queryOptions<MetaCampaignResponse, MetaCampaignError, MetaCampaignResponse, ReturnType<typeof metaCampaignQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await metaCampaign({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: metaCampaignQueryKey(options)
});

export const metaAdsetsQueryKey = (options: Options<MetaAdsetsData>) => createQueryKey('metaAdsets', options);

/**
 * List Meta ad sets
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Lists the ad sets under the connection’s Meta ad account, with each ad set’s status, parent campaign, budgets, and optimization (budgets in the account currency’s minor units).
 */
export const metaAdsetsOptions = (options: Options<MetaAdsetsData>) => queryOptions<MetaAdsetsResponse, MetaAdsetsError, MetaAdsetsResponse, ReturnType<typeof metaAdsetsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await metaAdsets({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: metaAdsetsQueryKey(options)
});

export const metaAdsetQueryKey = (options: Options<MetaAdsetData>) => createQueryKey('metaAdset', options);

/**
 * Retrieve a Meta ad set
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Retrieves a single ad set under the connection’s Meta ad account by its ad set id — status, parent campaign, budgets, and optimization.
 */
export const metaAdsetOptions = (options: Options<MetaAdsetData>) => queryOptions<MetaAdsetResponse, MetaAdsetError, MetaAdsetResponse, ReturnType<typeof metaAdsetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await metaAdset({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: metaAdsetQueryKey(options)
});

export const metaAdsQueryKey = (options: Options<MetaAdsData>) => createQueryKey('metaAds', options);

/**
 * List Meta ads
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Lists the ads under the connection’s Meta ad account, with each ad’s status, parent ad set and campaign ids, and timestamps.
 */
export const metaAdsOptions = (options: Options<MetaAdsData>) => queryOptions<MetaAdsResponse, MetaAdsError, MetaAdsResponse, ReturnType<typeof metaAdsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await metaAds({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: metaAdsQueryKey(options)
});

export const metaAdQueryKey = (options: Options<MetaAdData>) => createQueryKey('metaAd', options);

/**
 * Retrieve a Meta ad
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Retrieves a single ad under the connection’s Meta ad account by its ad id — status, parent ad set and campaign ids, and timestamps.
 */
export const metaAdOptions = (options: Options<MetaAdData>) => queryOptions<MetaAdResponse, MetaAdError, MetaAdResponse, ReturnType<typeof metaAdQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await metaAd({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: metaAdQueryKey(options)
});

export const googleGetAccountQueryKey = (options: Options<GoogleGetAccountData>) => createQueryKey('googleGetAccount', options);

/**
 * Retrieve the connected Google Ads account
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Returns the Google Ads account (the `customer` resource) backing this connection — id, descriptive name, currency, time zone, status, and whether it is a manager (MCC) or test account.
 */
export const googleGetAccountOptions = (options: Options<GoogleGetAccountData>) => queryOptions<GoogleGetAccountResponse, GoogleGetAccountError, GoogleGetAccountResponse, ReturnType<typeof googleGetAccountQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleGetAccount({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleGetAccountQueryKey(options)
});

/**
 * Edit Google Ads account URL settings
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Updates the account-level (customer) URL tracking settings — tracking_url_template and final_url_suffix. A campaign-level template overrides the account one. Pass `dry_run: true` to validate against Google without applying.
 */
export const googleEditAccountMutation = (options?: Partial<Options<GoogleEditAccountData>>): UseMutationOptions<GoogleEditAccountResponse, GoogleEditAccountError, Options<GoogleEditAccountData>> => {
    const mutationOptions: UseMutationOptions<GoogleEditAccountResponse, GoogleEditAccountError, Options<GoogleEditAccountData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleEditAccount({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Google Ads performance insights
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Performance metrics for the connection’s account, at a chosen level (account / campaign / ad group / ad / keyword), for a date range, with optional breakdowns. Leaf levels (ad / keyword) require an `ad_group_id`; campaign / ad group accept an optional `campaign_id`. Each row carries its `parent_id`, so the tree is assemblable. The flagship cross-checked read; use `/gaql` for anything outside this shape.
 */
export const googleGetInsightsMutation = (options?: Partial<Options<GoogleGetInsightsData>>): UseMutationOptions<GoogleGetInsightsResponse, GoogleGetInsightsError, Options<GoogleGetInsightsData>> => {
    const mutationOptions: UseMutationOptions<GoogleGetInsightsResponse, GoogleGetInsightsError, Options<GoogleGetInsightsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleGetInsights({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const googleGetAdTreeQueryKey = (options: Options<GoogleGetAdTreeData>) => createQueryKey('googleGetAdTree', options);

/**
 * Google Ads campaign tree
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * The campaign → ad_group → ad/keyword tree as flat, native, typed nodes — group them by `parent_id` to assemble the tree. Without `campaign_id`, returns every campaign (all channel types) with a per-type `child_kind`/`expandable` hint. With `campaign_id`, returns that campaign’s whole supported subtree (ad groups + the type’s leaf — keywords for Search, ads for Display/Video/Demand Gen) in one batched fetch at a single window. By default REMOVED nodes are hidden at every level; pass `status` to filter to specific serving states (ENABLED / PAUSED / REMOVED). Each node carries `budget_micros` (campaign nodes only), Google’s authoritative per-level metrics (no client roll-up), `cost_micros`, and `cost` (account currency).
 */
export const googleGetAdTreeOptions = (options: Options<GoogleGetAdTreeData>) => queryOptions<GoogleGetAdTreeResponse, GoogleGetAdTreeError, GoogleGetAdTreeResponse, ReturnType<typeof googleGetAdTreeQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleGetAdTree({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleGetAdTreeQueryKey(options)
});

/**
 * Run a raw GAQL query
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Runs a raw Google Ads Query Language (GAQL) query against the connection’s account and returns the native rows. Read-only (GAQL only does `SELECT`). The power-user escape hatch beneath the typed reads.
 */
export const googleRunGaqlMutation = (options?: Partial<Options<GoogleRunGaqlData>>): UseMutationOptions<GoogleRunGaqlResponse, GoogleRunGaqlError, Options<GoogleRunGaqlData>> => {
    const mutationOptions: UseMutationOptions<GoogleRunGaqlResponse, GoogleRunGaqlError, Options<GoogleRunGaqlData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleRunGaql({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Create a Google Ads campaign budget
 *
 * **Authentication.** Secret `pr_` API key only — this endpoint cannot be called with a browser token.
 *
 * Creates a standalone daily campaign budget (the `campaign_budget` resource) on the connection’s account. Money write — secret-key-only. Pass `dry_run: true` to validate against Google without creating. Returns the new resource id + resource_name to chain into a campaign.
 */
export const googleCreateBudgetMutation = (options?: Partial<Options<GoogleCreateBudgetData>>): UseMutationOptions<GoogleCreateBudgetResponse, GoogleCreateBudgetError, Options<GoogleCreateBudgetData>> => {
    const mutationOptions: UseMutationOptions<GoogleCreateBudgetResponse, GoogleCreateBudgetError, Options<GoogleCreateBudgetData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleCreateBudget({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Edit a Google Ads campaign budget
 *
 * **Authentication.** Secret `pr_` API key only — this endpoint cannot be called with a browser token.
 *
 * Updates a budget’s daily amount (in micros) by numeric id. Money write — secret-key-only. Pass `dry_run: true` to validate against Google without applying.
 */
export const googleEditBudgetMutation = (options?: Partial<Options<GoogleEditBudgetData>>): UseMutationOptions<GoogleEditBudgetResponse, GoogleEditBudgetError, Options<GoogleEditBudgetData>> => {
    const mutationOptions: UseMutationOptions<GoogleEditBudgetResponse, GoogleEditBudgetError, Options<GoogleEditBudgetData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleEditBudget({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Remove a Google Ads campaign budget
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Removes a campaign budget by numeric id (Google’s native remove). Set `dry_run: true` in the body to validate the removal against Google without applying it.
 */
export const googleRemoveBudgetMutation = (options?: Partial<Options<GoogleRemoveBudgetData>>): UseMutationOptions<GoogleRemoveBudgetResponse, GoogleRemoveBudgetError, Options<GoogleRemoveBudgetData>> => {
    const mutationOptions: UseMutationOptions<GoogleRemoveBudgetResponse, GoogleRemoveBudgetError, Options<GoogleRemoveBudgetData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleRemoveBudget({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const googleListCampaignsQueryKey = (options: Options<GoogleListCampaignsData>) => createQueryKey('googleListCampaigns', options);

/**
 * List Google Ads campaigns
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Returns the campaigns (the `campaign` resource) on the connection’s account — id, name, status, advertising channel type, and bidding strategy type.
 */
export const googleListCampaignsOptions = (options: Options<GoogleListCampaignsData>) => queryOptions<GoogleListCampaignsResponse, GoogleListCampaignsError, GoogleListCampaignsResponse, ReturnType<typeof googleListCampaignsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleListCampaigns({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleListCampaignsQueryKey(options)
});

/**
 * Create a Google Ads campaign
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Creates a Search or Display campaign (the `campaign` resource) on the connection’s account, attached to an existing budget. Pass `dry_run: true` to validate against Google without creating. Returns the new resource id + resource_name.
 */
export const googleCreateCampaignMutation = (options?: Partial<Options<GoogleCreateCampaignData>>): UseMutationOptions<GoogleCreateCampaignResponse, GoogleCreateCampaignError, Options<GoogleCreateCampaignData>> => {
    const mutationOptions: UseMutationOptions<GoogleCreateCampaignResponse, GoogleCreateCampaignError, Options<GoogleCreateCampaignData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleCreateCampaign({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const googleGetCampaignQueryKey = (options: Options<GoogleGetCampaignData>) => createQueryKey('googleGetCampaign', options);

/**
 * Retrieve a Google Ads campaign
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Returns a single campaign (the `campaign` resource) by its numeric id on the connection’s account — id, name, status, advertising channel type, and bidding strategy type.
 */
export const googleGetCampaignOptions = (options: Options<GoogleGetCampaignData>) => queryOptions<GoogleGetCampaignResponse, GoogleGetCampaignError, GoogleGetCampaignResponse, ReturnType<typeof googleGetCampaignQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleGetCampaign({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleGetCampaignQueryKey(options)
});

/**
 * Edit a Google Ads campaign
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Updates a campaign’s non-money fields — its name and URL tracking settings (tracking_url_template, final_url_suffix, url_custom_parameters) — by numeric id. Status has its own pause/enable endpoints; bids are a separate money write. Pass `dry_run: true` to validate against Google without applying.
 */
export const googleEditCampaignMutation = (options?: Partial<Options<GoogleEditCampaignData>>): UseMutationOptions<GoogleEditCampaignResponse, GoogleEditCampaignError, Options<GoogleEditCampaignData>> => {
    const mutationOptions: UseMutationOptions<GoogleEditCampaignResponse, GoogleEditCampaignError, Options<GoogleEditCampaignData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleEditCampaign({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Remove a Google Ads campaign
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Removes a campaign by numeric id (Google’s native remove). Set `dry_run: true` in the body to validate the removal against Google without applying it.
 */
export const googleRemoveCampaignMutation = (options?: Partial<Options<GoogleRemoveCampaignData>>): UseMutationOptions<GoogleRemoveCampaignResponse, GoogleRemoveCampaignError, Options<GoogleRemoveCampaignData>> => {
    const mutationOptions: UseMutationOptions<GoogleRemoveCampaignResponse, GoogleRemoveCampaignError, Options<GoogleRemoveCampaignData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleRemoveCampaign({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Pause a Google Ads campaign
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Pauses a campaign by its numeric id (reversible). Pass `dry_run: true` to preview without applying. Returns the new status and whether it executed.
 */
export const googlePauseCampaignMutation = (options?: Partial<Options<GooglePauseCampaignData>>): UseMutationOptions<GooglePauseCampaignResponse, GooglePauseCampaignError, Options<GooglePauseCampaignData>> => {
    const mutationOptions: UseMutationOptions<GooglePauseCampaignResponse, GooglePauseCampaignError, Options<GooglePauseCampaignData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googlePauseCampaign({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Enable a Google Ads campaign
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Enables (resumes) a campaign by its numeric id. Pass `dry_run: true` to preview without applying. Returns the new status and whether it executed.
 */
export const googleEnableCampaignMutation = (options?: Partial<Options<GoogleEnableCampaignData>>): UseMutationOptions<GoogleEnableCampaignResponse, GoogleEnableCampaignError, Options<GoogleEnableCampaignData>> => {
    const mutationOptions: UseMutationOptions<GoogleEnableCampaignResponse, GoogleEnableCampaignError, Options<GoogleEnableCampaignData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleEnableCampaign({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const googleSearchGeoTargetsQueryKey = (options: Options<GoogleSearchGeoTargetsData>) => createQueryKey('googleSearchGeoTargets', options);

/**
 * Search Google Ads geo targets
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Resolves a free-text location name (e.g. "New York") to matching geo target constants — each with id, name, canonical name, country code, target type, and status. Use a result’s `resource_name` (e.g. `geoTargetConstants/2840`), NOT the bare `id`, as the `geo_target_constant` of a location criterion.
 */
export const googleSearchGeoTargetsOptions = (options: Options<GoogleSearchGeoTargetsData>) => queryOptions<GoogleSearchGeoTargetsResponse, GoogleSearchGeoTargetsError, GoogleSearchGeoTargetsResponse, ReturnType<typeof googleSearchGeoTargetsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleSearchGeoTargets({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleSearchGeoTargetsQueryKey(options)
});

export const googleListCampaignCriteriaQueryKey = (options: Options<GoogleListCampaignCriteriaData>) => createQueryKey('googleListCampaignCriteria', options);

/**
 * List Google Ads campaign targeting criteria
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Lists a campaign’s targeting criteria. LOCATION and LANGUAGE are fully modelled (geo_target_constant / language_constant, status, negative flag); any other criterion type is returned as { type, criterion_id }.
 */
export const googleListCampaignCriteriaOptions = (options: Options<GoogleListCampaignCriteriaData>) => queryOptions<GoogleListCampaignCriteriaResponse, GoogleListCampaignCriteriaError, GoogleListCampaignCriteriaResponse, ReturnType<typeof googleListCampaignCriteriaQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleListCampaignCriteria({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleListCampaignCriteriaQueryKey(options)
});

/**
 * Add a Google Ads campaign targeting criterion
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Adds a geo (location) or language targeting criterion to a campaign. Pass `dry_run: true` to validate against Google without creating. Returns the new resource id + resource_name.
 */
export const googleCreateCampaignCriterionMutation = (options?: Partial<Options<GoogleCreateCampaignCriterionData>>): UseMutationOptions<GoogleCreateCampaignCriterionResponse, GoogleCreateCampaignCriterionError, Options<GoogleCreateCampaignCriterionData>> => {
    const mutationOptions: UseMutationOptions<GoogleCreateCampaignCriterionResponse, GoogleCreateCampaignCriterionError, Options<GoogleCreateCampaignCriterionData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleCreateCampaignCriterion({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Remove a Google Ads campaign targeting criterion
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Removes a campaign targeting criterion by its criterion id (Google’s native remove). Set `dry_run: true` in the body to validate the removal against Google without applying it.
 */
export const googleRemoveCampaignCriterionMutation = (options?: Partial<Options<GoogleRemoveCampaignCriterionData>>): UseMutationOptions<GoogleRemoveCampaignCriterionResponse, GoogleRemoveCampaignCriterionError, Options<GoogleRemoveCampaignCriterionData>> => {
    const mutationOptions: UseMutationOptions<GoogleRemoveCampaignCriterionResponse, GoogleRemoveCampaignCriterionError, Options<GoogleRemoveCampaignCriterionData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleRemoveCampaignCriterion({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Set a Google Ads campaign’s geo target type
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Sets a campaign’s geo target type — presence (people in the location) vs presence-or-interest (also people interested in it) — for positive and/or excluded locations. Pass `dry_run: true` to validate without applying.
 */
export const googleSetCampaignGeoSettingsMutation = (options?: Partial<Options<GoogleSetCampaignGeoSettingsData>>): UseMutationOptions<GoogleSetCampaignGeoSettingsResponse, GoogleSetCampaignGeoSettingsError, Options<GoogleSetCampaignGeoSettingsData>> => {
    const mutationOptions: UseMutationOptions<GoogleSetCampaignGeoSettingsResponse, GoogleSetCampaignGeoSettingsError, Options<GoogleSetCampaignGeoSettingsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleSetCampaignGeoSettings({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const googleListAdGroupsQueryKey = (options: Options<GoogleListAdGroupsData>) => createQueryKey('googleListAdGroups', options);

/**
 * List Google Ads ad groups
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Lists the ad groups (Google’s equivalent of an ad set) in the Google Ads account backing this connection — id, name, status, type, and parent campaign. Pass `campaign_id` to return only the ad groups under one campaign. By default REMOVED ad groups are hidden; pass `status` to filter to specific serving states (ENABLED / PAUSED / REMOVED).
 */
export const googleListAdGroupsOptions = (options: Options<GoogleListAdGroupsData>) => queryOptions<GoogleListAdGroupsResponse, GoogleListAdGroupsError, GoogleListAdGroupsResponse, ReturnType<typeof googleListAdGroupsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleListAdGroups({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleListAdGroupsQueryKey(options)
});

/**
 * Create a Google Ads ad group
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Creates an ad group (the `ad_group` resource) under a campaign on the connection’s account. Pass `dry_run: true` to validate against Google without creating. Returns the new resource id + resource_name to chain into ads and keywords.
 */
export const googleCreateAdGroupMutation = (options?: Partial<Options<GoogleCreateAdGroupData>>): UseMutationOptions<GoogleCreateAdGroupResponse, GoogleCreateAdGroupError, Options<GoogleCreateAdGroupData>> => {
    const mutationOptions: UseMutationOptions<GoogleCreateAdGroupResponse, GoogleCreateAdGroupError, Options<GoogleCreateAdGroupData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleCreateAdGroup({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const googleGetAdGroupQueryKey = (options: Options<GoogleGetAdGroupData>) => createQueryKey('googleGetAdGroup', options);

/**
 * Retrieve a Google Ads ad group
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Returns a single ad group by its numeric id from the Google Ads account backing this connection — id, name, status, type, and parent campaign.
 */
export const googleGetAdGroupOptions = (options: Options<GoogleGetAdGroupData>) => queryOptions<GoogleGetAdGroupResponse, GoogleGetAdGroupError, GoogleGetAdGroupResponse, ReturnType<typeof googleGetAdGroupQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleGetAdGroup({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleGetAdGroupQueryKey(options)
});

/**
 * Edit a Google Ads ad group
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Updates an ad group’s non-money fields (its name) by numeric id. Status has its own pause/enable endpoints; bids are a separate money write. Pass `dry_run: true` to validate without applying.
 */
export const googleEditAdGroupMutation = (options?: Partial<Options<GoogleEditAdGroupData>>): UseMutationOptions<GoogleEditAdGroupResponse, GoogleEditAdGroupError, Options<GoogleEditAdGroupData>> => {
    const mutationOptions: UseMutationOptions<GoogleEditAdGroupResponse, GoogleEditAdGroupError, Options<GoogleEditAdGroupData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleEditAdGroup({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Set a Google Ads ad group’s bids
 *
 * **Authentication.** Secret `pr_` API key only — this endpoint cannot be called with a browser token.
 *
 * Sets an ad group’s manual bids (cpc/cpm/target CPA in micros, target ROAS as a ratio) by numeric id. Money write — secret-key-only. Pass `dry_run: true` to validate without applying.
 */
export const googleSetAdGroupBidsMutation = (options?: Partial<Options<GoogleSetAdGroupBidsData>>): UseMutationOptions<GoogleSetAdGroupBidsResponse, GoogleSetAdGroupBidsError, Options<GoogleSetAdGroupBidsData>> => {
    const mutationOptions: UseMutationOptions<GoogleSetAdGroupBidsResponse, GoogleSetAdGroupBidsError, Options<GoogleSetAdGroupBidsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleSetAdGroupBids({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Remove a Google Ads ad group
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Removes an ad group by numeric id (Google’s native remove). Set `dry_run: true` in the body to validate the removal against Google without applying it.
 */
export const googleRemoveAdGroupMutation = (options?: Partial<Options<GoogleRemoveAdGroupData>>): UseMutationOptions<GoogleRemoveAdGroupResponse, GoogleRemoveAdGroupError, Options<GoogleRemoveAdGroupData>> => {
    const mutationOptions: UseMutationOptions<GoogleRemoveAdGroupResponse, GoogleRemoveAdGroupError, Options<GoogleRemoveAdGroupData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleRemoveAdGroup({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Pause a Google Ads ad group
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Pauses an ad group by its numeric id (reversible). Pass `dry_run: true` to preview without applying. Returns the new status and whether it executed.
 */
export const googlePauseAdGroupMutation = (options?: Partial<Options<GooglePauseAdGroupData>>): UseMutationOptions<GooglePauseAdGroupResponse, GooglePauseAdGroupError, Options<GooglePauseAdGroupData>> => {
    const mutationOptions: UseMutationOptions<GooglePauseAdGroupResponse, GooglePauseAdGroupError, Options<GooglePauseAdGroupData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googlePauseAdGroup({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Enable a Google Ads ad group
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Enables (resumes) an ad group by its numeric id. Pass `dry_run: true` to preview without applying. Returns the new status and whether it executed.
 */
export const googleEnableAdGroupMutation = (options?: Partial<Options<GoogleEnableAdGroupData>>): UseMutationOptions<GoogleEnableAdGroupResponse, GoogleEnableAdGroupError, Options<GoogleEnableAdGroupData>> => {
    const mutationOptions: UseMutationOptions<GoogleEnableAdGroupResponse, GoogleEnableAdGroupError, Options<GoogleEnableAdGroupData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleEnableAdGroup({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const googleListAdGroupCriteriaQueryKey = (options: Options<GoogleListAdGroupCriteriaData>) => createQueryKey('googleListAdGroupCriteria', options);

/**
 * List Google Ads ad-group targeting criteria
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Lists an ad group’s targeting criteria. The four demographics (AGE_RANGE, GENDER, INCOME_RANGE, PARENTAL_STATUS) are fully modelled (with their value, status, and negative flag); any other criterion type (e.g. KEYWORD) is returned as { type, criterion_id }.
 */
export const googleListAdGroupCriteriaOptions = (options: Options<GoogleListAdGroupCriteriaData>) => queryOptions<GoogleListAdGroupCriteriaResponse, GoogleListAdGroupCriteriaError, GoogleListAdGroupCriteriaResponse, ReturnType<typeof googleListAdGroupCriteriaQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleListAdGroupCriteria({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleListAdGroupCriteriaQueryKey(options)
});

/**
 * Add a Google Ads ad-group targeting criterion
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Adds a demographic (age range, gender, income range, or parental status) or audience (user list, audience, or custom audience) targeting criterion to an ad group. Audience criteria reference the `resource_name` returned by the audiences discovery read (`GET .../audiences`). Set `negative: true` to exclude instead of target. Pass `dry_run: true` to validate against Google without creating. Returns the new resource id + resource_name.
 */
export const googleCreateAdGroupCriterionMutation = (options?: Partial<Options<GoogleCreateAdGroupCriterionData>>): UseMutationOptions<GoogleCreateAdGroupCriterionResponse, GoogleCreateAdGroupCriterionError, Options<GoogleCreateAdGroupCriterionData>> => {
    const mutationOptions: UseMutationOptions<GoogleCreateAdGroupCriterionResponse, GoogleCreateAdGroupCriterionError, Options<GoogleCreateAdGroupCriterionData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleCreateAdGroupCriterion({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Remove a Google Ads ad-group targeting criterion
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Removes an ad-group targeting criterion by its criterion id (Google’s native remove). Set `dry_run: true` in the body to validate the removal against Google without applying it.
 */
export const googleRemoveAdGroupCriterionMutation = (options?: Partial<Options<GoogleRemoveAdGroupCriterionData>>): UseMutationOptions<GoogleRemoveAdGroupCriterionResponse, GoogleRemoveAdGroupCriterionError, Options<GoogleRemoveAdGroupCriterionData>> => {
    const mutationOptions: UseMutationOptions<GoogleRemoveAdGroupCriterionResponse, GoogleRemoveAdGroupCriterionError, Options<GoogleRemoveAdGroupCriterionData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleRemoveAdGroupCriterion({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const googleListAudiencesQueryKey = (options: Options<GoogleListAudiencesData>) => createQueryKey('googleListAudiences', options);

/**
 * List Google Ads audiences available to target
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Lists the user lists and audiences available to attach to an ad group, each with the `resource_name` you pass as the user_list / audience of an ad-group audience criterion. Optional `q` filters by name; `kind` narrows to one audience kind.
 */
export const googleListAudiencesOptions = (options: Options<GoogleListAudiencesData>) => queryOptions<GoogleListAudiencesResponse, GoogleListAudiencesError, GoogleListAudiencesResponse, ReturnType<typeof googleListAudiencesQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleListAudiences({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleListAudiencesQueryKey(options)
});

/**
 * Set a Google Ads campaign’s targeting setting
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Sets a campaign’s targeting setting for one dimension (e.g. AUDIENCE): Observation (`bid_only: true` — does NOT narrow reach) vs Targeting (`bid_only: false` — narrows reach to the targeted criteria). Campaign + dimension scoped: shared by every audience criterion in the campaign. Pass `dry_run: true` to validate without applying. Note: Google rejects setting this on a campaign whose ad group already has a targeting setting (and vice versa).
 */
export const googleSetCampaignTargetingSettingMutation = (options?: Partial<Options<GoogleSetCampaignTargetingSettingData>>): UseMutationOptions<GoogleSetCampaignTargetingSettingResponse, GoogleSetCampaignTargetingSettingError, Options<GoogleSetCampaignTargetingSettingData>> => {
    const mutationOptions: UseMutationOptions<GoogleSetCampaignTargetingSettingResponse, GoogleSetCampaignTargetingSettingError, Options<GoogleSetCampaignTargetingSettingData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleSetCampaignTargetingSetting({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const googleListAdsQueryKey = (options: Options<GoogleListAdsData>) => createQueryKey('googleListAds', options);

/**
 * List Google Ads ads
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Returns the ads (the `ad_group_ad` resource) on the connection’s account — id, name, creative type, serving status, and the owning ad group. Pass `ad_group_id` to return only the ads under one ad group. By default REMOVED ads are hidden; pass `status` to filter to specific serving states (ENABLED / PAUSED / REMOVED).
 */
export const googleListAdsOptions = (options: Options<GoogleListAdsData>) => queryOptions<GoogleListAdsResponse, GoogleListAdsError, GoogleListAdsResponse, ReturnType<typeof googleListAdsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleListAds({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleListAdsQueryKey(options)
});

/**
 * Create a Google Ads responsive search ad
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Creates a Responsive Search Ad (the `ad_group_ad` resource, inline text — no image assets) under an ad group on the connection’s account. Pass `dry_run: true` to validate against Google without creating. Returns the new resource id + resource_name.
 */
export const googleCreateAdMutation = (options?: Partial<Options<GoogleCreateAdData>>): UseMutationOptions<GoogleCreateAdResponse, GoogleCreateAdError, Options<GoogleCreateAdData>> => {
    const mutationOptions: UseMutationOptions<GoogleCreateAdResponse, GoogleCreateAdError, Options<GoogleCreateAdData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleCreateAd({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const googleGetAdQueryKey = (options: Options<GoogleGetAdData>) => createQueryKey('googleGetAd', options);

/**
 * Retrieve a Google Ads ad
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Returns a single ad (the `ad_group_ad` resource) by its composite id ({ad_group_id}~{ad_id}) on the connection’s account — id, name, creative type, serving status, and the owning ad group.
 */
export const googleGetAdOptions = (options: Options<GoogleGetAdData>) => queryOptions<GoogleGetAdResponse, GoogleGetAdError, GoogleGetAdResponse, ReturnType<typeof googleGetAdQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleGetAd({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleGetAdQueryKey(options)
});

/**
 * Create a Google Ads responsive display ad
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Creates a Responsive Display Ad (the `ad_group_ad` resource) under a Display ad group on the connection’s account, assembled from already-uploaded image-asset resource_names (upload them first via POST /v1/google/{connection_id}/assets). Pass `dry_run: true` to validate against Google without creating. Returns the new resource id + resource_name.
 */
export const googleCreateDisplayAdMutation = (options?: Partial<Options<GoogleCreateDisplayAdData>>): UseMutationOptions<GoogleCreateDisplayAdResponse, GoogleCreateDisplayAdError, Options<GoogleCreateDisplayAdData>> => {
    const mutationOptions: UseMutationOptions<GoogleCreateDisplayAdResponse, GoogleCreateDisplayAdError, Options<GoogleCreateDisplayAdData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleCreateDisplayAd({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Remove a Google Ads ad
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Removes an ad (an `ad_group_ad`) by its composite id ({ad_group_id}~{ad_id}) (Google’s native remove). Set `dry_run: true` in the body to validate the removal against Google without applying it.
 */
export const googleRemoveAdMutation = (options?: Partial<Options<GoogleRemoveAdData>>): UseMutationOptions<GoogleRemoveAdResponse, GoogleRemoveAdError, Options<GoogleRemoveAdData>> => {
    const mutationOptions: UseMutationOptions<GoogleRemoveAdResponse, GoogleRemoveAdError, Options<GoogleRemoveAdData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleRemoveAd({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Pause a Google Ads ad
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Pauses an ad by its composite id ({ad_group_id}~{ad_id}) (reversible). Pass `dry_run: true` to preview without applying. Returns the new status and whether it executed.
 */
export const googlePauseAdMutation = (options?: Partial<Options<GooglePauseAdData>>): UseMutationOptions<GooglePauseAdResponse, GooglePauseAdError, Options<GooglePauseAdData>> => {
    const mutationOptions: UseMutationOptions<GooglePauseAdResponse, GooglePauseAdError, Options<GooglePauseAdData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googlePauseAd({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Enable a Google Ads ad
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Enables (resumes) an ad by its composite id ({ad_group_id}~{ad_id}). Pass `dry_run: true` to preview without applying. Returns the new status and whether it executed.
 */
export const googleEnableAdMutation = (options?: Partial<Options<GoogleEnableAdData>>): UseMutationOptions<GoogleEnableAdResponse, GoogleEnableAdError, Options<GoogleEnableAdData>> => {
    const mutationOptions: UseMutationOptions<GoogleEnableAdResponse, GoogleEnableAdError, Options<GoogleEnableAdData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleEnableAd({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const googleListKeywordsQueryKey = (options: Options<GoogleListKeywordsData>) => createQueryKey('googleListKeywords', options);

/**
 * List Google Ads keywords
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Lists the keywords (`ad_group_criterion` of type KEYWORD) in the connection’s account — each with its criterion id, text, match type, status, negative flag, and owning ad group. Pass `ad_group_id` to return only the keywords under one ad group. By default REMOVED keywords are hidden; pass `status` to filter to specific serving states (ENABLED / PAUSED / REMOVED).
 */
export const googleListKeywordsOptions = (options: Options<GoogleListKeywordsData>) => queryOptions<GoogleListKeywordsResponse, GoogleListKeywordsError, GoogleListKeywordsResponse, ReturnType<typeof googleListKeywordsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleListKeywords({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleListKeywordsQueryKey(options)
});

/**
 * Create a Google Ads keyword
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Creates a keyword (an `ad_group_criterion` of type KEYWORD) under an ad group on the connection’s account. Pass `dry_run: true` to validate against Google without creating. Returns the new resource id + resource_name.
 */
export const googleCreateKeywordMutation = (options?: Partial<Options<GoogleCreateKeywordData>>): UseMutationOptions<GoogleCreateKeywordResponse, GoogleCreateKeywordError, Options<GoogleCreateKeywordData>> => {
    const mutationOptions: UseMutationOptions<GoogleCreateKeywordResponse, GoogleCreateKeywordError, Options<GoogleCreateKeywordData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleCreateKeyword({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const googleGetKeywordQueryKey = (options: Options<GoogleGetKeywordData>) => createQueryKey('googleGetKeyword', options);

/**
 * Retrieve a Google Ads keyword
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Returns one keyword (`ad_group_criterion` of type KEYWORD) by its composite id ({ad_group_id}~{criterion_id}), from the connection’s account — text, match type, status, negative flag, and owning ad group.
 */
export const googleGetKeywordOptions = (options: Options<GoogleGetKeywordData>) => queryOptions<GoogleGetKeywordResponse, GoogleGetKeywordError, GoogleGetKeywordResponse, ReturnType<typeof googleGetKeywordQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleGetKeyword({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleGetKeywordQueryKey(options)
});

/**
 * Set a Google Ads keyword’s bid
 *
 * **Authentication.** Secret `pr_` API key only — this endpoint cannot be called with a browser token.
 *
 * Sets a keyword’s manual CPC bid (in micros) by its composite id ({ad_group_id}~{criterion_id}). Money write — secret-key-only. Pass `dry_run: true` to validate without applying.
 */
export const googleSetKeywordBidMutation = (options?: Partial<Options<GoogleSetKeywordBidData>>): UseMutationOptions<GoogleSetKeywordBidResponse, GoogleSetKeywordBidError, Options<GoogleSetKeywordBidData>> => {
    const mutationOptions: UseMutationOptions<GoogleSetKeywordBidResponse, GoogleSetKeywordBidError, Options<GoogleSetKeywordBidData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleSetKeywordBid({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Remove a Google Ads keyword
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Removes a keyword (an `ad_group_criterion`) by its composite id ({ad_group_id}~{criterion_id}) (Google’s native remove). Set `dry_run: true` in the body to validate the removal against Google without applying it.
 */
export const googleRemoveKeywordMutation = (options?: Partial<Options<GoogleRemoveKeywordData>>): UseMutationOptions<GoogleRemoveKeywordResponse, GoogleRemoveKeywordError, Options<GoogleRemoveKeywordData>> => {
    const mutationOptions: UseMutationOptions<GoogleRemoveKeywordResponse, GoogleRemoveKeywordError, Options<GoogleRemoveKeywordData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleRemoveKeyword({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Pause a Google Ads keyword
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Pauses a keyword (`ad_group_criterion`) by its composite id ({ad_group_id}~{criterion_id}) (reversible). Pass `dry_run: true` to preview without applying. Returns the new status and whether it executed.
 */
export const googlePauseKeywordMutation = (options?: Partial<Options<GooglePauseKeywordData>>): UseMutationOptions<GooglePauseKeywordResponse, GooglePauseKeywordError, Options<GooglePauseKeywordData>> => {
    const mutationOptions: UseMutationOptions<GooglePauseKeywordResponse, GooglePauseKeywordError, Options<GooglePauseKeywordData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googlePauseKeyword({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Enable a Google Ads keyword
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Enables (resumes) a keyword (`ad_group_criterion`) by its composite id ({ad_group_id}~{criterion_id}). Pass `dry_run: true` to preview without applying. Returns the new status and whether it executed.
 */
export const googleEnableKeywordMutation = (options?: Partial<Options<GoogleEnableKeywordData>>): UseMutationOptions<GoogleEnableKeywordResponse, GoogleEnableKeywordError, Options<GoogleEnableKeywordData>> => {
    const mutationOptions: UseMutationOptions<GoogleEnableKeywordResponse, GoogleEnableKeywordError, Options<GoogleEnableKeywordData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleEnableKeyword({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const googleListConversionActionsQueryKey = (options: Options<GoogleListConversionActionsData>) => createQueryKey('googleListConversionActions', options);

/**
 * List Google Ads conversion actions
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Returns the conversion actions (the `conversion_action` resource) on the connection’s account — id, name, status, type, category, origin, primary-goal flag, counting type, value settings, and tag snippets.
 */
export const googleListConversionActionsOptions = (options: Options<GoogleListConversionActionsData>) => queryOptions<GoogleListConversionActionsResponse, GoogleListConversionActionsError, GoogleListConversionActionsResponse, ReturnType<typeof googleListConversionActionsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleListConversionActions({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleListConversionActionsQueryKey(options)
});

/**
 * Create a Google Ads conversion action
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Creates a conversion action (the `conversion_action` resource) — a website tag (WEBPAGE) or an offline/API-reported action (UPLOAD_CLICKS) — on the connection’s account. Pass `dry_run: true` to validate against Google without creating. Returns the new resource id + resource_name.
 */
export const googleCreateConversionActionMutation = (options?: Partial<Options<GoogleCreateConversionActionData>>): UseMutationOptions<GoogleCreateConversionActionResponse, GoogleCreateConversionActionError, Options<GoogleCreateConversionActionData>> => {
    const mutationOptions: UseMutationOptions<GoogleCreateConversionActionResponse, GoogleCreateConversionActionError, Options<GoogleCreateConversionActionData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleCreateConversionAction({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const googleGetConversionActionQueryKey = (options: Options<GoogleGetConversionActionData>) => createQueryKey('googleGetConversionAction', options);

/**
 * Retrieve a Google Ads conversion action
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Returns a single conversion action (the `conversion_action` resource) by its numeric id on the connection’s account.
 */
export const googleGetConversionActionOptions = (options: Options<GoogleGetConversionActionData>) => queryOptions<GoogleGetConversionActionResponse, GoogleGetConversionActionError, GoogleGetConversionActionResponse, ReturnType<typeof googleGetConversionActionQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleGetConversionAction({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleGetConversionActionQueryKey(options)
});

/**
 * Update a Google Ads conversion action
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Updates the mutable fields of a conversion action (name, category, status, value settings, counting type, lookback windows, primary-for-goal) by numeric id. Only the supplied fields change. Pass `dry_run: true` to validate against Google without applying.
 */
export const googleUpdateConversionActionMutation = (options?: Partial<Options<GoogleUpdateConversionActionData>>): UseMutationOptions<GoogleUpdateConversionActionResponse, GoogleUpdateConversionActionError, Options<GoogleUpdateConversionActionData>> => {
    const mutationOptions: UseMutationOptions<GoogleUpdateConversionActionResponse, GoogleUpdateConversionActionError, Options<GoogleUpdateConversionActionData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleUpdateConversionAction({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Remove a Google Ads conversion action
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Removes a conversion action by numeric id (Google’s native remove — a soft delete to status REMOVED). Set `dry_run: true` in the body to validate the removal against Google without applying it.
 */
export const googleRemoveConversionActionMutation = (options?: Partial<Options<GoogleRemoveConversionActionData>>): UseMutationOptions<GoogleRemoveConversionActionResponse, GoogleRemoveConversionActionError, Options<GoogleRemoveConversionActionData>> => {
    const mutationOptions: UseMutationOptions<GoogleRemoveConversionActionResponse, GoogleRemoveConversionActionError, Options<GoogleRemoveConversionActionData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleRemoveConversionAction({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Send Google Ads conversion events
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Sends a batch of conversion events (1–2000) for one conversion action on the connection’s account, via the Data Manager API. Plaintext identifiers (email / phone / address) are hashed server-side. Pass `dry_run: true` to validate against Google without sending. Returns a request id; poll the status read with it for per-reason processing results.
 */
export const googleSendConversionsMutation = (options?: Partial<Options<GoogleSendConversionsData>>): UseMutationOptions<GoogleSendConversionsResponse, GoogleSendConversionsError, Options<GoogleSendConversionsData>> => {
    const mutationOptions: UseMutationOptions<GoogleSendConversionsResponse, GoogleSendConversionsError, Options<GoogleSendConversionsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleSendConversions({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const googleGetConversionStatusQueryKey = (options: Options<GoogleGetConversionStatusData>) => createQueryKey('googleGetConversionStatus', options);

/**
 * Retrieve Google Ads conversion send status
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Returns the per-destination, count-by-reason processing status for a prior conversion send (by its `request_id`) — the total events submitted, the error breakdown (with typed codes), and informational warnings. `destinations` is EMPTY for the first ~minute after a send (Google registers the request asynchronously) — poll until it populates.
 */
export const googleGetConversionStatusOptions = (options: Options<GoogleGetConversionStatusData>) => queryOptions<GoogleGetConversionStatusResponse, GoogleGetConversionStatusError, GoogleGetConversionStatusResponse, ReturnType<typeof googleGetConversionStatusQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleGetConversionStatus({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleGetConversionStatusQueryKey(options)
});

export const googleListConversionRequestsQueryKey = (options: Options<GoogleListConversionRequestsData>) => createQueryKey('googleListConversionRequests', options);

/**
 * List Google Ads conversion sends
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Returns the history of conversion sends made on this connection (newest first) — each with its `request_id`, the conversion action it targeted, and how many events you submitted. Poll the status read with a `request_id` for that send’s per-destination processing result.
 */
export const googleListConversionRequestsOptions = (options: Options<GoogleListConversionRequestsData>) => queryOptions<GoogleListConversionRequestsResponse, GoogleListConversionRequestsError, GoogleListConversionRequestsResponse, ReturnType<typeof googleListConversionRequestsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleListConversionRequests({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleListConversionRequestsQueryKey(options)
});

export const googleListConversionRequestsInfiniteQueryKey = (options: Options<GoogleListConversionRequestsData>): QueryKey<Options<GoogleListConversionRequestsData>> => createQueryKey('googleListConversionRequests', options, true);

/**
 * List Google Ads conversion sends
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Returns the history of conversion sends made on this connection (newest first) — each with its `request_id`, the conversion action it targeted, and how many events you submitted. Poll the status read with a `request_id` for that send’s per-destination processing result.
 */
export const googleListConversionRequestsInfiniteOptions = (options: Options<GoogleListConversionRequestsData>) => infiniteQueryOptions<GoogleListConversionRequestsResponse, GoogleListConversionRequestsError, InfiniteData<GoogleListConversionRequestsResponse>, QueryKey<Options<GoogleListConversionRequestsData>>, number | Pick<QueryKey<Options<GoogleListConversionRequestsData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<GoogleListConversionRequestsData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                offset: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await googleListConversionRequests({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleListConversionRequestsInfiniteQueryKey(options)
});

export const googleListConversionGoalsQueryKey = (options: Options<GoogleListConversionGoalsData>) => createQueryKey('googleListConversionGoals', options);

/**
 * List Google Ads conversion goals
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:read` scoped to the profile that owns the connection.
 *
 * Returns the conversion goals (the `customer_conversion_goal` resource) on the connection’s account — category, origin, and whether each is biddable. Google auto-creates these per category × origin.
 */
export const googleListConversionGoalsOptions = (options: Options<GoogleListConversionGoalsData>) => queryOptions<GoogleListConversionGoalsResponse, GoogleListConversionGoalsError, GoogleListConversionGoalsResponse, ReturnType<typeof googleListConversionGoalsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await googleListConversionGoals({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: googleListConversionGoalsQueryKey(options)
});

/**
 * Set a Google Ads conversion goal’s biddable flag
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Sets whether a conversion goal (selected by category × origin) is biddable — a primary goal Google optimises toward, or a secondary observation-only goal. Pass `dry_run: true` to preview without applying. Returns the goal and whether it executed.
 */
export const googleSetConversionGoalMutation = (options?: Partial<Options<GoogleSetConversionGoalData>>): UseMutationOptions<GoogleSetConversionGoalResponse, GoogleSetConversionGoalError, Options<GoogleSetConversionGoalData>> => {
    const mutationOptions: UseMutationOptions<GoogleSetConversionGoalResponse, GoogleSetConversionGoalError, Options<GoogleSetConversionGoalData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleSetConversionGoal({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Upload a Google Ads image asset
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `ads:write` scoped to the profile that owns the connection.
 *
 * Uploads a Postrun image asset (its validated Google Ads rendition) to the connection’s account as an `image` asset, returning the new resource id + resource_name to chain into an ad/asset assembly. Pass `dry_run: true` to validate against Google without creating anything (Google’s native validate_only). The media asset must have a ready Google Ads rendition, else a 409.
 */
export const googleUploadImageAssetMutation = (options?: Partial<Options<GoogleUploadImageAssetData>>): UseMutationOptions<GoogleUploadImageAssetResponse, GoogleUploadImageAssetError, Options<GoogleUploadImageAssetData>> => {
    const mutationOptions: UseMutationOptions<GoogleUploadImageAssetResponse, GoogleUploadImageAssetError, Options<GoogleUploadImageAssetData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await googleUploadImageAsset({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const tiktokCreatorInfoQueryKey = (options: Options<TiktokCreatorInfoData>) => createQueryKey('tiktokCreatorInfo', options);

/**
 * Retrieve TikTok creator info
 *
 * **Authentication.** Secret `pr_` API key, or a browser-safe frontend token granting `connections:read` scoped to the profile that owns the connection.
 *
 * Retrieves the TikTok creator's publish options live from TikTok — required by TikTok's Content Posting policy to render the publishing UI: the creator nickname + avatar, the privacy options to offer (with no default), and the Comment/Duet/Stitch flags to disable. Returns 404 if the connection does not exist in your account or is not a TikTok connection, 409 if the TikTok account must be reconnected, and 502 if TikTok’s creator-info call fails.
 */
export const tiktokCreatorInfoOptions = (options: Options<TiktokCreatorInfoData>) => queryOptions<TiktokCreatorInfoResponse, TiktokCreatorInfoError, TiktokCreatorInfoResponse, ReturnType<typeof tiktokCreatorInfoQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await tiktokCreatorInfo({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: tiktokCreatorInfoQueryKey(options)
});

export const logsListQueryKey = (options?: Options<LogsListData>) => createQueryKey('logsList', options);

/**
 * List request logs
 *
 * **Authentication.** Secret `pr_` API key only — this endpoint cannot be called with a browser token.
 *
 * Lists the API activity for your account — one entry per action, newest first. Filter by profile, connection, platform, status, source, action, or time range.
 */
export const logsListOptions = (options?: Options<LogsListData>) => queryOptions<LogsListResponse, LogsListError, LogsListResponse, ReturnType<typeof logsListQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await logsList({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: logsListQueryKey(options)
});

export const logsListInfiniteQueryKey = (options?: Options<LogsListData>): QueryKey<Options<LogsListData>> => createQueryKey('logsList', options, true);

/**
 * List request logs
 *
 * **Authentication.** Secret `pr_` API key only — this endpoint cannot be called with a browser token.
 *
 * Lists the API activity for your account — one entry per action, newest first. Filter by profile, connection, platform, status, source, action, or time range.
 */
export const logsListInfiniteOptions = (options?: Options<LogsListData>) => infiniteQueryOptions<LogsListResponse, LogsListError, InfiniteData<LogsListResponse>, QueryKey<Options<LogsListData>>, number | Pick<QueryKey<Options<LogsListData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<LogsListData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                offset: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await logsList({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: logsListInfiniteQueryKey(options)
});

export const logsGetQueryKey = (options: Options<LogsGetData>) => createQueryKey('logsGet', options);

/**
 * Retrieve a request log
 *
 * **Authentication.** Secret `pr_` API key only — this endpoint cannot be called with a browser token.
 *
 * Retrieves a single request log entry by id.
 */
export const logsGetOptions = (options: Options<LogsGetData>) => queryOptions<LogsGetResponse, LogsGetError, LogsGetResponse, ReturnType<typeof logsGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await logsGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: logsGetQueryKey(options)
});

export const webhooksListEndpointsQueryKey = (options?: Options<WebhooksListEndpointsData>) => createQueryKey('webhooksListEndpoints', options);

/**
 * List webhook endpoints
 *
 * **Authentication.** Secret `pr_` API key only — this endpoint cannot be called with a browser token.
 *
 * Lists the webhook endpoints registered for your account.
 */
export const webhooksListEndpointsOptions = (options?: Options<WebhooksListEndpointsData>) => queryOptions<WebhooksListEndpointsResponse, WebhooksListEndpointsError, WebhooksListEndpointsResponse, ReturnType<typeof webhooksListEndpointsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await webhooksListEndpoints({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: webhooksListEndpointsQueryKey(options)
});

export const webhooksListEndpointsInfiniteQueryKey = (options?: Options<WebhooksListEndpointsData>): QueryKey<Options<WebhooksListEndpointsData>> => createQueryKey('webhooksListEndpoints', options, true);

/**
 * List webhook endpoints
 *
 * **Authentication.** Secret `pr_` API key only — this endpoint cannot be called with a browser token.
 *
 * Lists the webhook endpoints registered for your account.
 */
export const webhooksListEndpointsInfiniteOptions = (options?: Options<WebhooksListEndpointsData>) => infiniteQueryOptions<WebhooksListEndpointsResponse, WebhooksListEndpointsError, InfiniteData<WebhooksListEndpointsResponse>, QueryKey<Options<WebhooksListEndpointsData>>, number | Pick<QueryKey<Options<WebhooksListEndpointsData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
// @ts-ignore
{
    queryFn: async ({ pageParam, queryKey, signal }) => {
        // @ts-ignore
        const page: Pick<QueryKey<Options<WebhooksListEndpointsData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
            query: {
                offset: pageParam
            }
        };
        const params = createInfiniteParams(queryKey, page);
        const { data } = await webhooksListEndpoints({
            ...options,
            ...params,
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: webhooksListEndpointsInfiniteQueryKey(options)
});

/**
 * Create a webhook endpoint
 *
 * **Authentication.** Secret `pr_` API key only — this endpoint cannot be called with a browser token.
 *
 * Registers a URL to receive the chosen event types. Delivery, retries, and signing are handled for you.
 */
export const webhooksCreateEndpointMutation = (options?: Partial<Options<WebhooksCreateEndpointData>>): UseMutationOptions<WebhooksCreateEndpointResponse, WebhooksCreateEndpointError, Options<WebhooksCreateEndpointData>> => {
    const mutationOptions: UseMutationOptions<WebhooksCreateEndpointResponse, WebhooksCreateEndpointError, Options<WebhooksCreateEndpointData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await webhooksCreateEndpoint({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Delete a webhook endpoint
 *
 * **Authentication.** Secret `pr_` API key only — this endpoint cannot be called with a browser token.
 *
 * Deletes a webhook endpoint. Delivery to it stops immediately.
 */
export const webhooksDeleteEndpointMutation = (options?: Partial<Options<WebhooksDeleteEndpointData>>): UseMutationOptions<WebhooksDeleteEndpointResponse, WebhooksDeleteEndpointError, Options<WebhooksDeleteEndpointData>> => {
    const mutationOptions: UseMutationOptions<WebhooksDeleteEndpointResponse, WebhooksDeleteEndpointError, Options<WebhooksDeleteEndpointData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await webhooksDeleteEndpoint({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const webhooksGetEndpointQueryKey = (options: Options<WebhooksGetEndpointData>) => createQueryKey('webhooksGetEndpoint', options);

/**
 * Retrieve a webhook endpoint
 *
 * **Authentication.** Secret `pr_` API key only — this endpoint cannot be called with a browser token.
 *
 * Retrieves a single webhook endpoint by id.
 */
export const webhooksGetEndpointOptions = (options: Options<WebhooksGetEndpointData>) => queryOptions<WebhooksGetEndpointResponse, WebhooksGetEndpointError, WebhooksGetEndpointResponse, ReturnType<typeof webhooksGetEndpointQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await webhooksGetEndpoint({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: webhooksGetEndpointQueryKey(options)
});

/**
 * Update a webhook endpoint
 *
 * **Authentication.** Secret `pr_` API key only — this endpoint cannot be called with a browser token.
 *
 * Updates a webhook endpoint. Send only the fields you want to change; provide at least one.
 */
export const webhooksUpdateEndpointMutation = (options?: Partial<Options<WebhooksUpdateEndpointData>>): UseMutationOptions<WebhooksUpdateEndpointResponse, WebhooksUpdateEndpointError, Options<WebhooksUpdateEndpointData>> => {
    const mutationOptions: UseMutationOptions<WebhooksUpdateEndpointResponse, WebhooksUpdateEndpointError, Options<WebhooksUpdateEndpointData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await webhooksUpdateEndpoint({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Create a webhook portal link
 *
 * **Authentication.** Secret `pr_` API key only — this endpoint cannot be called with a browser token.
 *
 * Mints a one-time App Portal URL where you (or your customer) self-serve webhook delivery logs and signing secrets.
 */
export const webhooksCreatePortalMutation = (options?: Partial<Options<WebhooksCreatePortalData>>): UseMutationOptions<WebhooksCreatePortalResponse, WebhooksCreatePortalError, Options<WebhooksCreatePortalData>> => {
    const mutationOptions: UseMutationOptions<WebhooksCreatePortalResponse, WebhooksCreatePortalError, Options<WebhooksCreatePortalData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await webhooksCreatePortal({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const webhooksListEventTypesQueryKey = (options?: Options<WebhooksListEventTypesData>) => createQueryKey('webhooksListEventTypes', options);

/**
 * List webhook event types
 *
 * **Authentication.** Secret `pr_` API key only — this endpoint cannot be called with a browser token.
 *
 * Lists the event types you can subscribe an endpoint to (the catalog).
 */
export const webhooksListEventTypesOptions = (options?: Options<WebhooksListEventTypesData>) => queryOptions<WebhooksListEventTypesResponse, WebhooksListEventTypesError, WebhooksListEventTypesResponse, ReturnType<typeof webhooksListEventTypesQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await webhooksListEventTypes({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: webhooksListEventTypesQueryKey(options)
});

/**
 * Send a test event
 *
 * **Authentication.** Secret `pr_` API key only — this endpoint cannot be called with a browser token.
 *
 * Emits a `webhook.ping` event to your subscribed endpoints so you can verify delivery end to end.
 */
export const webhooksPingMutation = (options?: Partial<Options<WebhooksPingData>>): UseMutationOptions<WebhooksPingResponse, WebhooksPingError, Options<WebhooksPingData>> => {
    const mutationOptions: UseMutationOptions<WebhooksPingResponse, WebhooksPingError, Options<WebhooksPingData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await webhooksPing({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Mint a scoped frontend token
 *
 * **Authentication.** Secret `pr_` API key only — this endpoint cannot be called with a browser token.
 *
 * Mints a short-lived, profile-scoped token from your secret `pr_` key. Call this from your backend; the returned token is safe to hand to a browser, which then calls the Postrun API directly. The secret key never touches the browser.
 */
export const tokensMintMutation = (options?: Partial<Options<TokensMintData>>): UseMutationOptions<TokensMintResponse, TokensMintError, Options<TokensMintData>> => {
    const mutationOptions: UseMutationOptions<TokensMintResponse, TokensMintError, Options<TokensMintData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await tokensMint({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};
