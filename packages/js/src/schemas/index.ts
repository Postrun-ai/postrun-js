/**
 * @postrun/js/schemas — client-side validation (no HTTP round-trip).
 *
 * PLACEHOLDER. These validators will be generated from the public OpenAPI spec
 * so a customer can validate a post in the browser exactly the way the API's
 * `dry_run` would, with zero network cost. Deferred until the client + React
 * layer land — see the package README ("Client-side validation").
 */
export {};
